{
  "name": "codecanvas-desktop",
  "version": "0.0.0",
  "description": "Electron shell for CodeCanvas AI",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^42.0.0",
    "typescript": "~5.9.3"
  }
}
